This package uses the compiler API to
perform compilation of a java program
loaded into memory as a string, and then
to run the bytecode. See the usage
of this package in HaltingCalculator examples
to see how it works.