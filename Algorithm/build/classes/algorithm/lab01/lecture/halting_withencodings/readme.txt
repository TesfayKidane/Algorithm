This package builds a halting calculator that accepts
inputs (m, n) where m is an integer encoding of a  normal Java program
and n is an integer (BigInteger) input. 

Because encodings are used, it is possible to create
the SelfApp app that is capable of running on its own code.